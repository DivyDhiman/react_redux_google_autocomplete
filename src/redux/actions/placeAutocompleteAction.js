export const PLACE_AUTOCOMPLETE_CLEAR = () => ({});

export const PLACE_AUTOCOMPLETE_MARKER = (marker) => ({
    marker
});

export const PLACE_AUTOCOMPLETE_SEARCH_HISTORY = (lat, lng, address) => ({
    lat, lng, address
});

